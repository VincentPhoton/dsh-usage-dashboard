# TODO — 功能 backlog

格式：`[ ] 【功能名】痛点 / 验收标准 / 复杂度 / 优先级`

## 脑暴轮次 #1（2026-08-15）

- [x] **月度预算与进度条** — 痛点：没有「花超了没」的感觉。
  验收：设置本月预算 ¥X，显示已用/预算进度条 + 按当前速率预测的月底花费。复杂度 M。**P1**
- [x] **悬浮窗点击直达「额度」tab（能力边界：不做）** — 痛点：从悬浮窗看到余额后要手动切 view。
  验收结论：DSH 公开 client API 不暴露 view 切换；`setView` 只存在宿主私有 session store actions，避免用 DOM 文案点击做脆弱绑定。复杂度 S。**P1**
- [ ] **费用走势折线图** — 痛点：柱状图看单日，看不出累计花钱趋势。
  验收：近 30 天累计费用折线，hover 显示当日/累计。复杂度 M。**P2**
- [x] **导出用量报表 CSV / JSON** — 痛点：想拿数据自己做分析。
  验收：一键下载 `dsh-usage-YYYYMMDD.csv`（逐天 + 逐模型两张表）。复杂度 S。**P2**
- [ ] **悬浮窗精简模式** — 痛点：常驻窗口占地方。
  验收：可切到只剩「¥46.80」小药丸，再点回完整卡片；模式持久化。复杂度 S。**P2**

## 脑暴轮次 #2（2026-08-15，轮次 5 后）

新认知：实测缓存命中占 prompt tokens 的 98%，缓存命中价是未命中价的 1/120；峰谷价差 2 倍。
这两件事是 DSH 用户成本的真正杠杆，但界面上完全没体现。

- [ ] **今日花费速率外推** — 痛点：一天过半时想知道照这个速度今天会花多少。
  验收：今日卡显示「按当前速率预计全天 ¥X」。复杂度 S。**P2**

## 脑暴轮次 #3（2026-08-15，持续锻造重启）

- [x] **异常消耗侦测（产品决策：撤回）** — 只有本机、短周期日志时，正常的批量任务也会被包装成“异常”，
  阈值无法可靠区分真实风险和自然波动。为避免噪音、误导和虚假的确定性，服务端判定、接口字段与 UI 提示均已移除。复杂度 M。**P1**
- [x] **可切换统计周期** — 痛点：固定时间窗口无法同时满足近期排查与长期复盘。
  验收：图表与排行榜可切换 7 / 30 / 90 / 365 天，选择持久化，切换后汇总与空态口径一致。复杂度 M。**P1**
- [x] **数据新鲜度与同步状态** — 痛点：用户分不清当前数字是刚刷新、缓存结果还是刷新失败后的旧数据。
  验收：仪表盘显示「更新于 X 分钟前」；刷新中、最新、缓存回退三种状态视觉可辨，且不遮挡主要数据。复杂度 S。**P1**
- [ ] **会话排行可追溯** — 痛点：发现高成本会话后，无法快速回到对应对话定位原因。
  验收：排行项可打开对应 DSH 会话；若宿主不提供跳转 API，则提供复制会话 ID 的明确降级路径。复杂度 M。**P1**
- [ ] **周同比成本对照** — 痛点：只看今日/本月环比，难判断最近一周是否正在系统性变贵。
  验收：展示本周与上周同期花费、差值和按日对照；不足一周数据时明确说明口径。复杂度 M。**P2**
- [ ] **成本效率指标** — 痛点：总花费不能回答「这笔钱产出了多少有效 token」。
  验收：显示单次平均成本、每元输出 tokens、输出占比，并为除零与无数据提供清晰空态。复杂度 S。**P2**
- [ ] **价格情景模拟器** — 痛点：峰谷价、缓存命中率和模型组合变化对账单的影响难以心算。
  验收：可调整高峰占比、缓存命中率与模型组合，实时显示预测花费和可节省金额，不改动真实配置。复杂度 L。**P2**
- [ ] **模型级预算与预警** — 痛点：总预算正常时，单个昂贵模型仍可能悄悄失控。
  验收：可为各模型设置月度预算；达到 80% 与 100% 时分级提示，并显示剩余额度。复杂度 M。**P2**

## 脑暴轮次 #4（2026-08-15，完成 3 个迭代后）

- [x] **统计覆盖度诊断** — 痛点：费用只来自本机日志，但用户不知道漏掉了多少设备、时间段或损坏记录。
  验收：显示扫描会话数、最早/最新记录时间、跳过记录数与「仅本机 DSH 日志」口径；存在解析缺口时给出明确警示。复杂度 M。**P1**
- [ ] **会话排行搜索与展开** — 痛点：Top 6 之外的会话无法查找，定位某次昂贵对话仍要翻侧栏。
  验收：按标题或会话 ID 搜索，支持展开完整排行；无匹配结果有空态，默认仍保持紧凑 Top 6。复杂度 M。**P1**
- [ ] **余额变化快照** — 痛点：平台只返回当前余额，充值、赠送与消费造成的变化没有本地轨迹。
  验收：从启用功能后按刷新记录余额快照，展示最近变化与时间；明确说明不是平台账单，支持清空本地历史。复杂度 M。**P1**
- [ ] **峰谷新价倒计时** — 痛点：价格切换临近，用户容易错过 2026-08-17 生效节点。
  验收：生效前显示精确到小时的倒计时与预估增幅，生效后自动消失并切换为省钱建议。复杂度 S。**P2**
- [ ] **复制用量周报** — 痛点：向团队同步成本时只能截图或手工抄数字。
  验收：一键复制 Markdown 周报，含本周费用、环比、Top 模型/会话、缓存节省和统计口径；复制后有短暂成功反馈。复杂度 S。**P2**
- [ ] **预算提醒中心** — 痛点：只有打开额度 tab 才看得到预算风险，容易错过 80% / 100% 节点。
  验收：在悬浮窗展示未读预算风险，可逐条确认；同一阈值每自然月只提醒一次，不申请浏览器系统通知权限。复杂度 M。**P2**
- [ ] **仪表盘视图预设** — 痛点：频繁切换模型与统计周期时，每次都要重新勾选。
  验收：保存、重命名、删除视图预设；预设包含周期、模型过滤和图表维度，刷新后仍可恢复。复杂度 M。**P2**
- [ ] **日志空洞日历** — 痛点：热力图的 0 可能是真没用，也可能是本机日志缺失，当前无法区分。
  验收：结合会话日志覆盖范围标记「明确零用量」与「无法确认」两类日期，tooltip 解释原因。复杂度 M。**P2**

## 脑暴轮次 #5（2026-08-15，轮次 20–23 后）

新认知：周期口径已经统一，下一步价值最高的是把“看见变化”升级为“解释变化并采取动作”，同时补齐费用维度与项目归因。

- [x] **图表指标切换（tokens / 费用 / 调用）** — 痛点：当前柱图只能看 token 量，高缓存模型可能 token 很高但费用很低，容易误判。
  验收：逐天 / 逐小时图可切 tokens、估算费用、调用次数；tooltip、峰值单位和空态同步，选择持久化。复杂度 S。**P1**
- [ ] **单日成本钻取** — 痛点：热力图或柱图发现某天异常后，无法知道是哪几个模型和会话造成的。
  验收：点击有数据的日期后显示当日费用、模型贡献和 Top 会话；可关闭并回到原图表焦点，无数据日不打开空面板。复杂度 M。**P1**
- [ ] **当前会话成本护栏** — 痛点：长时间 agent 循环可能在用户意识到之前持续烧额度。
  验收：可设置单会话预警金额；当前会话跨过阈值时悬浮窗和额度页各提醒一次，明确只提醒、不自动中断任务。复杂度 L。**P1**
- [ ] **工作区成本归因** — 痛点：多个 workspace 共用同一 API Key 时，不知道哪个项目消耗最多。
  验收：按本地会话的 workspace 元数据聚合费用 / tokens / 调用；缺失 workspace 的会话归入“未识别”，总和与所选周期一致。复杂度 M。**P1**
- [ ] **自定义日期对比** — 痛点：固定 7/30/90/365 天仍无法比较发布前后或两个具体冲刺周期。
  验收：选择两个起止日期区间，显示费用、tokens、调用差值与 Top 模型变化；非法 / 重叠区间给出清晰提示。复杂度 L。**P2**
- [ ] **余额耗尽日期与可信区间** — 痛点：“还能用 21 天”没有具体日期，也没表达日用量波动带来的不确定性。
  验收：在可用天数旁显示预计日期，并基于近 7 / 30 天波动给出乐观—保守区间；样本不足时明确降级为单点估算。复杂度 S。**P2**
- [ ] **费用估算校准** — 痛点：本地日志估算与官方账单可能因缺失设备或价格变化存在系统偏差。
  验收：用户可录入某周期官方账单金额，显示估算误差与校准系数；校准只用于对照，不篡改原始估算，支持清除。复杂度 M。**P2**
- [ ] **模型替代节省估算** — 痛点：用户知道 pro 贵，但不知道历史任务若改用 flash 大约能省多少。
  验收：按所选周期将某模型用量套用另一模型价格，展示理论节省与适用限制；不暗示两模型效果等价。复杂度 M。**P2**

## 脑暴轮次 #6（2026-08-15，i18n 后 UI / UX 专项）

方向约束：不扩张复杂业务功能，只补视觉、交互、响应式与可访问性；优先让既有数据更容易读、更容易操作。

- [x] **图表键盘探索** — 痛点：柱图与热力图 tooltip 只能用指针触发，键盘用户读不到单点明细；若直接给 365 个点
  全部加 Tab 又会形成焦点陷阱。验收：每张图只占一个 Tab 入口，方向键在数据点间移动，焦点与 tooltip 同步，
  Home / End 可到首尾，切换周期后焦点仍合法。复杂度 M。**P1**
- [x] **图表触屏点按明细** — 痛点：移动端没有 hover，轻触数据点时 tooltip 不稳定。
  验收：点按柱或热力格可固定明细，再点同一点或图外关闭；滚动图表不误触，键盘行为不回归。复杂度 M。**P1**
- [x] **窄屏底部安全区** — 痛点：DSH 固定输入框会覆盖长仪表盘底部，最后一张设置卡滚到底时仍可能难以完整阅读。
  验收：620px 与更窄视口中，末卡可滚到输入框上方且无多余大空洞；桌面布局不增加无意义留白。复杂度 S。**P1**
- [ ] **紧凑控件触控热区统一** — 痛点：统计周期、图表指标、导出等紧凑按钮在触屏上命中区域偏小。
  验收：粗指针环境下主要控件最小高度 36px、间距不误触；鼠标环境保持 DSH 的紧凑密度。复杂度 S。**P2**
- [ ] **强制色彩模式适配** — 痛点：Windows forced-colors / 高对比度下状态点、柱图与热力格可能丢失语义。
  验收：强制色彩模式中可见焦点、选中态、柱形和告警边界均清晰；核心信息不只靠颜色表达。复杂度 M。**P2**
- [ ] **加载骨架尺寸对齐** — 痛点：首次加载结束时骨架与真实内容高度存在跳变，慢机器上更明显。
  验收：余额、汇总、柱图三类骨架与真实首屏占位差异控制在 8px 内；中英文切换不造成二次跳动。复杂度 S。**P2**
- [ ] **状态与刷新栏窄屏重排** — 痛点：缓存状态、失败说明与刷新按钮同时出现时，长英文在窄屏首行容易显得拥挤。
  验收：状态信息优先完整换行，刷新按钮固定可达但不挤压错误文案；320 / 480 / 620px 均无截断。复杂度 S。**P2**

## 脑暴轮次 #7（2026-08-16，排版设计专项）

方向约束：用户明确把重点收拢到**界面排版设计优化**（信息层级、留白节奏、栅格/对齐、卡片密度、
排版层级、响应式重排），交互细节打磨降为次优先，继续不扩张业务功能。以下候选来自对 dashboard.tsx /
widget.tsx / styles.ts 的真实截图（1440/1024/620/390px 四档视口 + 悬浮窗展开/收起态）+ 源码逐行核对
的排版审计，每条都能追溯到具体截图证据和 `styles.ts` 行号，不是空泛印象。

- [x] **悬浮窗折叠态标题截断**（轮次 34）— 痛点：收起后标题「DeepSeek 额度」被压缩成「DeepSe…」，
  折叠总额、刷新/展开等次要控件反而完整保留，最需要保留的身份标签被牺牲。
  验收：收起态下插件身份标签完整可读或改用不会截断的等效识别方式；折叠总额与标题不再挤同一行。复杂度 S。**P1**
- [x] **账户余额数字独立视觉权重**（轮次 35）— 痛点：最高优先级的余额数字与「近30天输入/输出/缓存命中」等
  次要分解数字共用同一 `.dq-stat-value{font-size:18px;font-weight:650}`，扫读时无法优先识别。
  验收：余额数字有独立于其他 Stat 复用场景的更大字号/字重；不影响 Stat 组件在其余位置的现有外观。复杂度 S。**P1**
- [x] **卡片容器建立视觉权重分级**（轮次 36）— 痛点：全站 9 张卡片共用同一套 `.dq-card`/`.dq-card-title`
  （padding 16px、标题 12px/600），眯眼测试下权重完全拉平，看不出账户余额与「官方平台」孰轻孰重。
  验收：至少区分「主卡片」（余额/消耗概览/用量趋势）与「次级分析卡片」两档权重；四档视口层级仍可辨识，
  不引入新配色体系。复杂度 M。**P1**
- [x] **卡片间距与卡内间距拉开疏密对比**（轮次 37）— 痛点：`.dq-balance{gap:16px}` 与 `.dq-card{padding:16px}`
  同值，卡片之间和卡片内部的呼吸感没有区别，谈不上"刻意的疏密节奏"。
  验收：卡片间距明显大于卡内小节间距（如 24px vs 12px），四档视口无横向溢出或跳变。复杂度 S。**P2**
- [x] **窄屏用量统计 3+1 断行修复**（轮次 38）— 痛点：620px 下「近30天输入/输出/缓存命中/模型调用」4 个统计值
  前 3 个同行、第 4 个独占一行并留大片空白（`dq-usage-totals` 走自动换行，未被 620px 媒体查询覆盖）。
  验收：620px 断点内该行改为可控的两列布局，不再依赖不受控的自动换行；320/480/620px 均无断行空洞。复杂度 S。**P2**
- [x] **合并官方平台卡片**（轮次 39）— 痛点：「官方平台」卡只放 3 个链接，高度 99.5px 全站最矮，却背着和
  550px「DSH 用量」卡同规格的边框标题，信息密度与卡片成本不匹配。
  验收：并入设置卡或降级为轻量行内条，减少一次卡片开合的认知成本；链接功能与可达性不变。复杂度 S。**P2**
- [x] **拆分超载的「DSH 用量」卡片**（轮次 40）— 痛点：用量总数、逐天/逐小时图表、热力图、覆盖诊断四个时间口径
  不同的子模块（30/90/365 天可变 vs 热力图固定近一年 vs 全量诊断）挤在一张卡里，高度达 550px，全站最臃肿。
  验收：拆成 2-3 张卡片，标题与内容一一对应；不改变任何统计口径、交互逻辑或既有单测。复杂度 M。**P2**
- [x] **字号档位收敛**（轮次 41）— 痛点：全站 12 档字号中有 4 档（11/11.5/12/12.5px）挤在 1.5px 区间内，
  合计 52 处使用，肉眼无法区分层级，只是维护负担；字重也有 650/680/700 三档难以分辨。
  验收：合并冗余档位（如把 11–12.5px 区间收敛到 2 档），视觉不倒退，同步清理选择器。复杂度 M。**P2**
- [x] **宽屏内容宽度机会评估**（轮次 42）— 痛点：1440px 视口下内容仍锁死 `max-width:860px` 居中，两侧各闲置
  约 290px，1024 与 1440 之间没有任何结构性差异。
  验收：先评估是否值得改（Operate 类工具单列内容本身是合理的克制设计）；如改动仅限提高 max-width 或让
  模型/会话排行两张分析卡在 ≥1200px 并排，不强行拉宽全部卡片。复杂度 M。**P2**

## 用户指定优先级（2026-08-15）

- [x] **中英文 i18n** — 痛点：当前全部硬编码中文，英文环境下与 DSH GUI 语言不一致。
  验收：默认跟随 DSH / 浏览器语言，支持中文与 English 手动切换并持久化；仪表盘、悬浮窗、图表 tooltip、
  加载 / 空 / 错误状态和无障碍文案全部覆盖，切换无需刷新。已通过 DSH 全局语言设置实测。复杂度 L。**P1**

> 产品方向：完成 i18n 后暂停扩张复杂业务功能，优先做 UI 视觉统一、信息层级、响应式、交互反馈与可访问性打磨。

## 已完成

- [x] **修复余额明细 hover 无内容（同款平台限制）**（轮次 47，轮次 43 已知问题的后续修复）— 轮次 43
  独立 QA 用真实鼠标 hover 发现未展开 `<details>` 的非 `summary` 子元素在当前 Chromium 下不会真正渲染
  （即使作者 CSS 设成 `display:block`），并确认「余额明细」`.dq-remaining`（轮次 29 起同款纯 CSS
  `:hover` 方案）同样中招。本轮把轮次 43 已验证的受控 `<details>` 方案（`remainingHovering`/
  `remainingManuallyOpened` 两个 state 驱动 `open`，`onPointerEnter`/`onPointerLeave` + `summary`
  `onClick` 里 `preventDefault` 自管 toggle）原样复刻到 `.dq-remaining`，`styles.ts` 删掉死代码的
  `:hover` CSS 分支。键盘/触屏/窄屏不遮挡/中英文均验证通过，未新增业务功能。
- [x] **会话排行加展开详情按钮**（轮次 46，用户直接反馈的新需求）— 「会话成本排行」每行原来只有
  标题/费用/占比条/tokens/调用/最后活动六个汇总字段，用户要求加一个可展开的按钮，展开内容要和轮次 45
  「当前会话消耗」卡片一样。每行改为原生 `<details>`（延续 `.dq-pricing`/`.dq-coverage` 已有的
  disclosure 语法），首次展开才按需 `fetchSessionUsage(id)`，收起再展开不重复请求（组件内按 id 缓存），
  展开区直接复用 `SessionUsageCard` 渲染完整明细（同一个组件，不是仿写）；加载态复用 `SessionUsageSkeleton`
  骨架屏，错误态复用 `.dq-empty--error` + `localizeApiError`。键盘 Tab+Enter/Space、触屏点按均为原生
  `<details>` 行为，未新增翻译键（复用轮次 45 已有词条），620px 无新增横向溢出。
- [x] **新增当前会话消耗卡片**（轮次 45，用户直接反馈的新需求）— 用户指出「额度」tab 挂在具体会话
  内部、和「对话」「对话轨迹」同层级，但内容全是账户全局维度，缺一张这个会话专属的消耗卡片。host 端
  新增 `fetchSessionUsage()` 与 `GET /api/dsh-usage-dashboard/session?id=` 路由（同一鉴权前缀，不接入
  TTL 缓存，跟随会话实时变化）；client 端新增卡片放在账户余额之后、消耗概览之前，标题明确写「当前
  会话消耗」避免和账户全局口径混淆，给 `.dq-card--primary` 主卡权重；内容复用 `Stat`/`.dq-balance-grid`
  展示三个headline数字、复用模型成本排行榜的 `.dq-rank`/`MODEL_COLORS` 展示按模型拆分明细、复用
  `CoverageDiagnostics` 的时间格式化展示活跃时间范围；加载态复用骨架屏语言，空态/错误态有清晰文案，
  不接入 localStorage 持久化缓存（会话专属数据、每会话缓存一份增加复杂度不划算）。中英文双语齐全。
- [x] **多选模型柱状图改堆叠**（轮次 44，用户直接反馈）— 「用量趋势」卡多选 2+ 个模型时，逐天/逐小时
  柱图原来是并列分组柱（每模型一根独立柱子，数量翻倍），改成堆叠柱：同一时间点的各模型融合进一根柱子，
  按 `series` 顺序从下到上堆叠，柱子总高度按"该时间点全部选中模型的合计值"与"所有时间点合计值的最大值"
  换算，不同柱子之间可比；每个模型分段仍是独立的可聚焦/hover/触屏点按元素，键盘 roving tabindex、
  桌面 hover、触屏 tap-to-pin、图例配色均未回归，620px 无新增横向溢出，中英文双语已验证。
- [x] **修复预计可用 hover 无内容**（轮次 43，用户直接反馈的 bug）— 账户余额卡「预计可用」数字之前
  只挂了原生 `title` 属性、容器是不可聚焦的 `<div>`，hover 没有可见反馈，键盘也无法触达；改成与轮次 29
  「余额明细」同款的 `<details><summary>` disclosure，保留桌面 hover 预览，点击/触屏/Enter/Space/
  `:focus-visible` 均可展开查看完整的「按近 N 天日均 ¥X 估算」说明，三列布局与中英文双语均未回归。
- [x] **宽屏内容宽度机会评估**（轮次 42）— 评估后判定不做。真实截图 + 实测：1440/1600/1920px 下
  `.dq-balance` 两侧留白分别占视口 39.7%/45.8%/54.8%，随视口线性增长；但同一 DSH 窗口 Chat 对话消息列
  实测在 1440px 与 1920px 下宽度完全相同（748px，不随视口缩放），说明宿主自身就是"定宽居中阅读列"惯例，
  插件 860px 封顶（比 Chat 748px 还宽）是延续而非偏离这个惯例。折中方案里"提高 max-width"会拉长全站
  说明性正文（`.dq-pricing-foot` 等，12px 无自身行长上限）的单行字符数，伤害次要文字可读性；"模型/会话
  排行两卡并排"要么把卡宽减半明显提高长标题截断概率，要么单独开宽子容器造成页面内宽度不一致，与轮次
  36-41 建立的统一卡片家族规范冲突。结论：留白是合理设计选择而非缺陷，不改动代码，与轮次 20「悬浮窗直达
  额度能力边界确认」同为"评估后判定不做"的有效交付。脑暴轮次 #7 全部 9 个候选（3 P1 + 6 P2）处理完毕。
- [x] **字号档位收敛**（轮次 41）— 重新审计后 11/11.5/12/12.5px 四档、共 53 处使用仍挤在 1.5px 区间内
  （原审计的"约 52 处"未过时）；按语义收敛为两档整数像素：11.5px（19 处次要说明性长文本）与 12.5px
  （2 处，`.dq-alert`/`.dq-empty` 告警与空态整句）并入 **12px**（与既有小节标题/按钮/正文同档，
  `.dq-window-btn` 借此与 `.dq-chart-switch-btn`/`.dq-model-btn` 这一"分段控件"家族第一次真正统一）；
  11px（13 处 dt 标签/徽章/占比）语义与合并组不同，原样保留，形成 11px/12px 两档、1px 整数级差。
  轮次 34/36/38 新增的 13px/15px 及本身独立的 22px hero 数字锚点均在区间外，未触碰；字重
  400/500/550/600/650/680/700 评估后判断 650/680/700 分别锚定轮次 35/36 已验证过的设计决策，保守不动。
- [x] **拆分超载的「DSH 用量」卡片**（轮次 40）— 拆成「用量趋势」（用量总数统计 + 逐天/逐小时图表 +
  模型筛选/统计周期/指标切换 + 覆盖诊断，保留 `.dq-card--primary` 主卡权重）与「用量热力图」（近一年热力图
  单独成卡，判定为次级分析卡）两张卡片，占据原「DSH 用量」卡片位置；统计周期切换控件随受影响内容留在
  「用量趋势」卡；卡片总数 8→9；不改变任何统计口径、聚合逻辑或图表/disclosure 组件内部实现。
- [x] **合并官方平台卡片**（轮次 39）— 「官方平台」3 个链接（查看额度/用量、生成 API Key、服务状态）
  整体并入「设置」卡，标题下加 `.dq-links-title` 小节标题保留"官方平台"文案，链接区与开关/预警线/
  预算设置之间复用卡内既有的 14px/`border-top` 分节手法隔开；卡片总数 9→8，顺序与内容归属不变，
  中英文翻译键、focus-visible 焦点环、620px 窄屏无新增溢出均保持。

- [x] **窄屏用量统计 3+1 断行修复**（轮次 38）— `.dq-usage-totals` 新增 620px 媒体查询，改用可控的
  2 列 2 行网格（`repeat(2,minmax(0,1fr))`，复用 `.dq-coverage-metrics` 已验证过的同款响应式模式），
  子项 `.dq-stat` 的 `min-width` 在网格内覆盖为 0 防止撑破窄列；320/480/620px 实测均无断行空洞，
  桌面横向排列不受影响，4 个统计值内容与颜色未变。

- [x] **卡片间距与卡内间距拉开疏密对比**（轮次 37）— `.dq-balance{gap:16px}` 改为 `gap:24px`，相对卡内
  小节分隔（`margin-top:12/14/16px`）拉开 1.5～2 倍级差，制造"卡片外疏、卡片内密"的对比；`.dq-card`/
  `.dq-card--primary` 的 padding（16px/20px，轮次 36 已定）未改动，620px 窄屏媒体查询不重复声明 `gap`，
  统一继承新值，纵向 gap 不影响横向宽度。

- [x] **卡片容器建立视觉权重分级**（轮次 36）— 账户余额 / 消耗概览 / DSH 用量三张主卡片新增修饰类
  `.dq-card--primary`，在 `.dq-card` 基线上叠加更大内边距（20px vs 16px）、更深一档的边框变量
  （`--dsw-alias-border-l2` vs `--dsw-alias-border-l1`）、更重的标题（13px/700/主色 vs 12px/600/次级色）；
  高峰/闲时、缓存命中、模型排行、会话排行四张次级分析卡片与官方平台、设置两张导航卡片都不改，维持原基线，
  形成两档权重，未引入新配色体系。

- [x] **账户余额数字独立视觉权重**（轮次 35）— 余额数字所在的 `.dq-remaining` 补上
  `font-size:22px;font-weight:680`（取自本站既有 hero 数字样式 `.dq-period-cost`，未新增字号档位）；
  同卡「预计可用」「状态」与用量卡分解数字仍是原有 `.dq-stat-value` 18px/650，悬浮窗展开态余额本就是
  独立的 24px/700，不需要跟随改动。

- [x] **悬浮窗折叠态标题截断**（轮次 34）— 折叠总额从标题所在行移出，改到标题行下方独立一行；
  收起态标题「DeepSeek 额度」不再与折叠总额挤同一行、不再被 ellipsis 截断，刷新 / 展开按钮不受影响。

- [x] **窄屏底部安全区**（轮次 33）— 620px 及更窄视口下，仪表盘底部 padding 改为随 DSH 固定输入框
  实测高度动态计算（`ResizeObserver` 复用 widget.tsx 已验证的 composer 选择器），设置卡可完整滚到
  输入框上方且不留过量空洞；桌面布局 padding 完全不变。

- [x] **图表触屏点按明细**（轮次 32）— 柱图、分模型柱图与热力图新增触屏 tap 路径：点按数据点固定 tooltip，
  再点同点或图表外区域关闭；用位移 + 耗时双阈值区分点按与滚动/拖动，不拦截页面滚动；桌面 hover 与
  轮次 31 键盘 roving tabindex 均未改动。

- [x] **图表键盘探索**（轮次 31）— 普通柱图、分模型柱图与热力图各只占一个 Tab 入口；方向键、Home / End
  在数据点间移动，焦点环、可访问名称与 tooltip 同步，底部点位会自动留出输入框安全距离。

- [x] **完整支持减少动态效果**（轮次 30）— `prefers-reduced-motion: reduce` 下关闭悬浮窗吸附、进度条、
  排行榜、控件和加载 / 刷新动画，并禁用平滑滚动；正常模式视觉不变。

- [x] **余额明细键盘 / 触屏可达**（轮次 29）— 把仅 hover 可见的充值 / 赠送额度改为原生 disclosure；
  鼠标、触屏、Enter / Space 均可切换，补齐双语操作提示与 2px 焦点环。

- [x] **消耗概览层级扁平化**（轮次 28）— 去掉外层卡片内三张重复浅底小卡；桌面以细分隔线组成一条
  可比较指标带，窄屏改为水平分隔的纵向列表，中英文与 620px 布局均已实测。

- [x] **中英文 i18n**（轮次 27）— 接入 DSH 原生 locale 服务与全局持久化语言设置；tab、悬浮窗、仪表盘、
  图表 tooltip、同步 / 缓存失败状态、空态、设置提示和无障碍文案全部覆盖，英文紧凑数字与窄屏布局同步适配。

- [x] **撤回异常消耗侦测**（轮次 26）— 完整移除服务端阈值判定、逐会话日聚合、API 字段、
  仪表盘提示和柱图警示标记；产品不再把不完整的本机日志解释成确定性风险。

- [x] **图表指标切换**（轮次 24）— tokens / 费用 / 调用三档持久化选择，统一驱动逐天、逐小时和
  分模型柱图；标题与峰值按指标切换单位，tooltip 始终保留日期、tokens、估算费用、调用四项上下文。

- [x] **可切换统计周期**（轮次 23）— 7 / 30 / 90 / 365 天四档持久化选择，统一联动 tokens 汇总、
  逐天 / 逐小时图、模型排行与会话排行；365 天逐日柱图可横向查看并自动定位到最新日期，
  标题、空态和导出说明均明确各自统计口径。

- [x] **模型筛选键盘闭环**（轮次 22）— 打开后自动聚焦首个选项，Tab + Space 可完成模型选择，
  Escape 关闭并把焦点还给触发按钮；补齐 `aria-expanded` / `aria-controls` / 分组标签，点击外部仍可关闭。

- [x] **统计覆盖度诊断**（轮次 21）— usage 响应附带本机日志扫描覆盖元数据；用量卡可展开查看
  成功扫描 / 读取失败 / 跳过记录、最早与最新用量时间，并明确排除其他设备、平台直接调用和已删除日志；
  存在损坏会话或异常 usage 记录时显示「存在缺口」并提示汇总可能偏低。

- [x] **导出用量报表 CSV / JSON**（轮次 20）— 用量卡可下载逐天 CSV、逐模型 CSV 和完整 JSON；
  文件日期按北京时间，CSV 防公式注入并正确转义，菜单支持 Escape / 点击外部关闭和下载成功反馈。

- [x] **悬浮窗直达额度能力边界确认**（轮次 20）— `shell.overlay` 没有 session store actions，
  `IConversation` 也不提供 `setView`；按候选原约定判定不做，避免绑定宿主 DOM 与当前语言。

- [x] **额度页不再被悬浮窗遮挡**（轮次 19）— 完整仪表盘挂载时悬浮窗临时淡出并禁用交互；
  切回 Chat / Trajectory 按原显示开关、位置与收起状态恢复，设置文案明确说明作用域。

- [x] **数据新鲜度与同步状态**（轮次 18）— 状态栏持续显示首次同步、同步中、已同步、缓存数据、
  缓存回退或同步失败，并复用真实 usage 缓存时间戳；强刷失败保留数据和时间，不伪装成最新。

- [x] **月度预算与进度条**（轮次 17）— 预算持久化；消耗概览显示已用/预算、剩余金额、
  进度条与按北京时间自然月已过比例外推的月底花费；未设置时可直达输入框，正常/风险/超支分级反馈。

- [x] **会话成本排行**（轮次 16）— Top 6 会话按费用降序，含标题（从 `session/title` 事件折叠而来，
  搭用量重放的顺风车，零额外读盘）、占比条、tokens / 调用次数 / 最后活动时间 / 占总费用比例。

- [x] **悬浮窗不再遮挡输入框**（轮次 14）— 拖动边界下沿排除 composer；`elementFromPoint` 实测输入框右侧控件可达。

- [x] **打磨轮**（轮次 13）— 卡片重排（「官方平台」下沉到导航区）、柱状图峰值标注、
  缓存「未命中」改用与高峰卡一致的琥珀色（未命中不是错误，是更贵）、
  悬浮窗改 border-box 且标题可省略号截断（收起态 285px → 270px）、
  全部可交互元素补 `:focus-visible` 焦点环。

- [x] **高峰 / 闲时分布 + 涨价影响预估**（轮次 12）— 占比条 + 两侧 tokens/调用数；
  生效日前显示「同样用量在新价下 ¥X（较现价 +Y%）」，生效后改为「挪到闲时可省 ¥Z」。

- [x] **余额低预警**（轮次 11）— 「设置」卡内可配阈值（默认 10，填 0 关闭，持久化）；
  低于阈值时仪表盘顶部出现警示条（含可用天数 + 去充值链接），悬浮窗状态点与余额数字同步转警示色。

- [x] **自定义图表 tooltip**（轮次 10）— 柱状图/分组柱状图/热力图共用跟随光标的深色 tooltip，
  即时显示、边缘自动收拢、离开即隐藏；柱子 hover 提亮、热力图格子 hover 描边；图例色块不再误吃 hover 样式。

- [x] **热力图坐标与图例**（轮次 9）— 左侧 一/三/五 星期标签、上方月份标签（列首次进入某月时标注）、
  右下角 少→多 五级色阶图例；窄容器内横向滚动而非撑破页面。

- [x] **悬浮窗显示今日花费**（轮次 8）— 展开态多一行「今日消耗 ¥X」，数据只读共享缓存，
  悬浮窗自身永不触发后台聚合；手动 ↻ 才拉取，hover 显示数据新鲜度。

- [x] **余额可用天数预估**（轮次 7）— 余额卡新增「预计可用 X 天」，按近 7 个自然日（含无用量的日子）
  的日均消耗折算，hover 说明口径；无用量时显示「—」，不足 3 天转警示色。

- [x] **缓存命中与节省卡**（轮次 6）— 命中率 + 已节省费用 + 若全部未命中的对照价 + 命中/未命中占比条；
  命中率 < 60% 时切换为改进建议文案。

- [x] **模型成本排行榜**（轮次 5）— 按费用降序列出模型的 ¥ / 占比条 / tokens / 调用次数 / 输入输出缓存拆分；
  同时统一模型配色：排行、下拉选择器、图表图例三处颜色由模型顺序决定（原来图例按勾选顺序上色）。

- [x] **按模型分别计价 + 峰谷定价适配**（轮次 4）— 新增 `src/pricing.ts`：pro / flash 两套价目、
  2026-08-17 00:00（北京时间）切换峰谷、逐条记录按模型与时段计价；UI 增加可展开的「计价说明」价目表。

- [x] **今日 / 本月消耗汇总卡**（轮次 3）— 新增「消耗概览」卡：今日 / 本月 / 累计 三窗口，
  今日带「较昨日」环比、本月带「较上月同期」环比；用量卡去掉重复的累计数字。

- [x] **加载 / 空 / 错误三态分离**（轮次 2）— 骨架屏取代假空态；余额与用量独立加载；
  刷新按钮 disabled + 转圈；部分失败给出人话提示并保留缓存数据。

- [x] **悬浮窗状态持久化**（轮次 1）— `visible` / `corner` / `collapsed` 存 localStorage，硬刷新后保持；
  顺带修掉刷新后吸附到左上角会盖住 Chat/Trajectory/额度 tab 栏的 P0（边界改用 main column + header 排除 + 自愈重定位）。
