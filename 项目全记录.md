# dsh-usage-dashboard 本地 fork 全记录

> 从上游项目诞生（2026-08-15）到本 fork 合并上游 0.6.0 并按官方价格表对齐（2026-09-11）的完整情况。
> 本文是**总览**；修正过程细节见《修正记录.md》，跨会话速查见《记忆.md》，上游自己的开发日志见 `PROGRESS.md` / `LEARNINGS.md` / `TODO.md`。

---

## 0. 一句话

上游 `@cassius0924/dsh-usage-dashboard` 把 DeepSeek 的账单搬进 DSH Web 的「额度」tab；
本 fork 在 2026-08-22 因**对账偏差 3 倍以上**做了 6 项修正，2026-09-11 合并上游 0.6.0（保住宿主新接口兼容与图片卡），
并按官方价格页把 09-10 之后的计价口径改回官方算式。当前 71/71 测试通过，已部署到 web profile。

---

## 1. 上游项目：它是什么、谁做的、走到哪

| 项 | 内容 |
|---|---|
| 仓库 | [Cassius0924/dsh-usage-dashboard](https://github.com/Cassius0924/dsh-usage-dashboard)（MIT） |
| 形态 | DSH Web 插件：宿主端（读本机会话日志 + `/user/balance`）+ 客户端（`conversation.view` 上的「额度」tab + 悬浮窗） |
| 时间线 | 基线 2026-08-15（轮次 0）→ `v0.4.0` 2026-08-16 → `v0.5.0` 2026-08-19 → `v0.6.0` 2026-09-10 |
| 开发方式 | 文档驱动、轮次制：每轮记 `PROGRESS.md`（做了什么/怎么验）、`LEARNINGS.md`（教训）、`TODO.md`（下一个脑暴轮次） |

上游 51 轮迭代做出来的东西（按 `PROGRESS.md`）：今日/本月/累计概览、按模型 + 峰谷分段计价、模型成本排行、
缓存命中与节省、余额可用天数、余额低预警、高峰/闲时分布、导出报表、统计覆盖度诊断、中英文 i18n、
会话成本排行、图表交互与无障碍、当前会话消耗卡、悬浮窗吸附与折叠、**图片/多模态用量卡**（0.6.0）、
额度页版本 footer（0.6.0）、**适配 handle 化 `sessionPersistence`**（0.6.0，针对宿主 0.1.5-rc.1 的破坏性重构）。

上游的定位是「看得见」，计价是**估算**：它按公开价目表估算，并用余额接口拉真实余额，但不承诺与账单逐分对齐。

---

## 2. fork 的起因：2026-08-22 的一次对账事故

真正用起来后，插件显示的数字与开放平台后台**对不上**：

| 指标 | 插件显示 | 平台后台 | 结论 |
|---|---|---|---|
| 今日调用 | 虚高约 3.4 倍 | 平台口径 | — |
| 今日费用 | 虚高约 3 倍 | 平台口径 | — |

逐项排查后定位出 **3 个真 bug + 1 个能力缺口**（外加后来发现的 2 个问题），全部修在本地。

---

## 3. 六项修正（细节见《修正记录.md》）

| # | 问题 | 根因 | 修复 | 结果 |
|---|---|---|---|---|
| ① | 周六 9-12 点被按高峰价算 | `isPeak()` 只看小时，不看星期/节假日 | 先判周末 → 再查 `CN_HOLIDAYS`（2024–2026 国务院日历）→ 再比高峰窗口 | 周末/节假日全天闲时 |
| ② | 今日调用虚高 ~3 倍 | 子代理会话**回放**父会话同一批事件（同 messageId），各会话简单相加 | `fetchUsage` 增加跨会话 `seenMessageIds` 去重 | 与平台口径吻合度 ~97–98% |
| ③ | 费用低估 ~27% | 闲时把**缓存命中价**也减半了 | `halved()` 只减 input/output（08-17 档口径，见 §7） | 今日费用与平台偏差 2–5% |
| ④ | 平台没有用量 API，看不到"平台口径"的花费 | — | 新增 `src/balance-tracker.ts`：**余额差值法**（今晨余额 − 当前余额 + 今日充值增量），基准文件 `~/.dsh/dsh-usage-dashboard-balance.json` 按北京时间零点重置；2026-08-22 首次播种当日基准（数值已脱敏） | 余额卡第 4 项「今日消耗（平台核算）」，与平台**0% 偏差** |
| ⑤ | 打开额度 tab 停在底部；偶发白屏 | 宿主所有会话视图共用一个滚动容器 `[data-conversation-scroll]` 且保留位置；切换瞬间视图可能被裁成 0 高 | 挂载时 `useLayoutEffect` 把共享滚动容器归零 + rAF 再断言；白屏时每 150ms 轻推 reflow，1.2s 预算内可见即停 | 打开即见余额卡，无白屏 |
| ⑥ | 跨天后「今日消耗」不重置（2026-08-23 发现） | 客户端缓存与渲染都没有"天"的概念，把昨天数字当今日 | 三层防御：`cache.ts` 加北京日 `sameDayOnly` 约束；渲染记录拉取时刻，非当日显示「—」；host 端凭证解析加 10s 有界超时 | 零点后第一次轮询自动重算，无需重启 |

**对账方法论**（可复用）：请求数/tokens 以本机会话日志为口径，费用以「余额差值」为权威值；
残余 2–5% 差异归因于本机日志之外（其他设备、直接调 API）。

---

## 4. 工程与运维配套（fork 能长期活着的原因）

- **`lib/` 构建产物入库 + profile 用 `file:` 依赖**：任何 `pnpm add/remove/install` 都不会把本地修复还原成 npm 上的版本。
- **部署注意**：构建产生新 inode（pnpm 的 directory 依赖是硬链接/复制），必须**重新复制 `lib/` 与 `package.json`** 到
  `~/.dsh/profiles/web/node_modules/@cassius0924/dsh-usage-dashboard/`；`pnpm install` 不会刷新。
- **备份**：profile 的 `package.json` 有 preinstall 钩子自动全量备份到 `~/Documents/DSH备份/dsh-auto-<时间戳>/`，
  另有手动脚本 `backup-dsh.sh`。**教训**：钩子不一定触发（2026-09-09 的一次 pnpm add 就没触发），每次操作后要人工确认出现新快照。
- **文档体系**：《修正记录.md》记"过程与对账"，《记忆.md》记"速查与环境硬规则"（本次新增本文《项目全记录.md》做总览）。
- **分工**：上游文件（`PROGRESS.md`/`LEARNINGS.md`/`TODO.md`）保持上游原样，fork 的叙述都写在中文文档里。

---

## 5. 2026-09-11 合并上游 0.6.0

**触发**：宿主 DSH 升到 **0.1.5-rc.1**，`sessionPersistence` 被 handle 化（`readFrom` 移除，改 `open(id,'read')` → 分片 `read` → `close`，
`list()` 返回 snapshot）。0.5.0 的 fork 上所有基于会话日志的视图会整体报 `persistence.readFrom is not a function` —— 不升不行。

**上游 0.6.0（tag `edf7c0a`，2026-09-10）带来**：era 化计价表（含 09-10 调价、09-14 pro→flash 改道）、图片/多模态用量卡、
handle 化接口两代兼容、额度 tab 挂载置顶、版本 footer。

**合并方式**：`git merge edf7c0a`（还原点 tag `fork-before-0.6.0-20260911-182327`，提交 `c452114`）。

**6 个文件冲突与取舍**：

| 文件 | 处理 |
|---|---|
| `src/pricing.ts` | 头部注释取并集；价目表用上游命名 + fork 注释；`isPeak()` 用 fork 规则（先判周末/节假日） |
| `src/usage.ts` | import 取并集；回调 = 上游 vision 累积器 **+** fork 的 messageId 去重 |
| `src/client/locales.ts` | 文案取并集，删掉已无引用的 `pricing.switchNote` |
| `src/client/dashboard.tsx` | 挂载置顶：保留 fork 白屏防御 + 上游的 `props.sessionId` 依赖 |
| `test/pricing.test.ts` | helper 兼容两种调用签名；用例取并集；闲时期望值按 fork 口径修正 |
| `README.md` | 计价条目取并集；单价表按 fork 口径写 |

另：`build-local.mjs` 补上 `__PLUGIN_LABEL__` define，与上游 `build.mjs` 对齐。
结果：**70/70 测试通过**，typecheck/build 全绿，部署后 `diff -r` 一致。

---

## 6. 2026-09-11 官方价格表对齐（提交 `321c760`）

**触发**：用户转来官方通知（09-10 12:00 flash 系列调价；09-14 12:00 V4 Pro 下线、路由 V4.1 Flash），
并给了官方价格页。抓取 [api-docs.deepseek.com/zh-cn/quick_start/pricing/](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/) 核对，官方原文：

> 空闲时段价格为高峰时段价格的一半。高峰时段为北京时间周一至周五 9:00 - 12:00、14:00 - 18:00（其余为空闲时段）。

| 模型 | | 空闲时段 | 高峰时段 |
|---|---|---|---|
| deepseek-flash（V4.1-Flash） | 缓存命中 / 未命中 / 输出 | 0.02 / 1 / 4 | 0.04 / 2 / 8 |
| deepseek-v4-pro（09-14 12:00 前） | 缓存命中 / 未命中 / 输出 | 0.15 / 4.5 / 13.5 | 0.3 / 9 / 27 |

**关键点**：官方是「**每一项都减半，含缓存命中价**」——这否定了 fork 原先"缓存命中价一律不减半"的外推。

**实现**：`PriceEra` 与 `halved()` 增加 `halveCacheHit` 开关：

- **09-10 及以后 = `true`**：三项全减半 = 官方口径（flash 空闲 0.02/1/4，pro 空闲 0.15/4.5/13.5）；
- **08-17 – 09-10 = `false`**：保留当年按**实际账单**核对出的"缓存命中价不减半"（该档若减半会低估约 27%）。

UI 计价说明与 README 单价表写回官方数字；新增用例逐项钉住官方六格单价。
结果：**71/71 通过**，运行时实算 `flash peak 0.04/2/8 offPeak 0.02/1/4 | pro peak 0.3/9/27 offPeak 0.15/4.5/13.5`，与官方逐格一致。

---

## 7. 当前状态（2026-09-11）

| 项 | 值 |
|---|---|
| 版本 / 提交 | `0.6.0`（与上游同号）/ `c452114`（合并）→ `321c760`（官方价格对齐），工作区干净 |
| 部署位置 | `~/.dsh/profiles/web/node_modules/@cassius0924/dsh-usage-dashboard/`（`lib/` 与仓库 md5 一致） |
| 测试 | `node test/run.mjs` 71/71；`tsc`（主 + 测试）exit 0；`pnpm run build` exit 0 |
| 备份 / 还原点 | tag `fork-before-0.6.0-20260911-182327`；`dsh-auto-20260911-182629`、`dsh-auto-20260911-184217` |
| 宿主 | DSH `0.1.5-rc.1`（插件按代兼容新旧 `sessionPersistence`） |
| 待办 | **重启 `dsh web`** 加载新 host bundle（用户选择自行重启）；重启后额度页应显示 footer `v0.6.0` 与官方单价 |

---

## 8. 文件地图

```
项目全记录.md              本文：全过程总览
修正记录.md               修正过程、对账数据、维护流程（第一手）
记忆.md                   跨会话速查卡 + 环境硬规则
PROGRESS.md / LEARNINGS.md / TODO.md  上游文档（保持原样）
src/pricing.ts            费率、时段、era、halveCacheHit、CN_HOLIDAYS、图片 token 估算
src/usage.ts              会话日志重放、messageId 去重、余额与平台核算、两代 persistence 读取
src/balance-tracker.ts    「今日消耗（平台核算）」余额差值法（fork 独有）
src/contract.ts           两端共享类型
src/context.ts            宿主接口的结构面（含两代 persistence）
src/client/dashboard.tsx  「额度」页 UI（滚动/白屏/多模态卡/footer）
src/client/cache.ts       客户端缓存（北京日 sameDayOnly）
src/client/api.ts         客户端取数（含 balanceAt 记录）
build.mjs                 完整构建（esbuild + tsc 声明，注入版本 footer）
build-local.mjs           快构建（跳过 tsc，同样注入 footer）
lib/                      构建产物（入库，供 file: 依赖直接安装）
```

---

## 9. 维护流程

1. 改 `src/**` 或 `test/**`
2. `pnpm run build`（或只改 JS 时 `node build-local.mjs`）
3. `node test/run.mjs`（当前 71/71）
4. 先备份（`bash ~/Documents/DSH备份/backup-dsh.sh "自动:..."`），再 `rm -rf $DST/lib && cp -R $SRC/lib $DST/lib`，
   并同步 `package.json`（等）
5. 生效：纯客户端改动硬刷新；host 端改动重启 `dsh web`
6. `git add -A && git commit`

**周期性事项**：`CN_HOLIDAYS` 只覆盖 2024–2026，国务院公布 2027 安排后补 `'MM-DD'`；
升级上游 = `git fetch upstream --tags && git merge <tag>`，冲突集中在 pricing / usage / locales / dashboard / tests / README，
**合并后必须逐条核对 fork 的口径有没有被上游改写**（本次就有两处：`halved()` 与 `isPeak()`）。

---

## 10. 已知风险与未验证事项（诚实清单）

1. **08-17–09-10 档的缓存命中价**仍是"不减半"（与官方算式不一致，但当年账单支持）——只影响 9 月 10 日以前的历史估算。
2. **法定节假日按空闲时段**是本 fork 的额外口径：官方原文只写「周一至周五」，没有节假日例外；尚无账单验证，
   2026-10-01 之后可用余额差值法复核。
3. **本地 / 第三方模型**会按 DeepSeek 官方价估算但实际不扣款（README 顶部已用醒目段落警示）。
4. **「今日消耗（平台核算）」依赖余额接口与本地基准文件**：基准未建立（新的一天首次拉取失败）时显示「—」而不是猜。
5. **只统计本机会话日志**：其他设备、直接调 API 的用量看不见（残余 2–5% 差异）。
6. **图片 token 是估算**（官方缩放规则、每张上限 384），并归属到随后一次模型调用；接口返回的用量才是权威。
7. **fork 与上游版本号同号**（都是 0.6.0），区分只能靠 git 历史与文档（UI footer 也显示 0.6.0）。
8. **平台没有用量 API**：费用永远是估算；唯一权威是官方账单与余额。

---

## 11. 结论：这套 fork 相对上游的"准"与"先进"

- **更准**（在本机负载上，有对账证据）：09-10 前用账单核对过的缓存价口径、08-23 前修掉 3 倍虚高、
  余额差值法给出 0% 偏差的当日消费、跨天不冒充昨日数据。上游 0.6.0 至今没有余额差值核算、没有跨会话去重、没有节假日规则。
- **更先进？不成立**：功能面几乎全是上游做的（多模态卡、era 计价、接口兼容、导出、i18n、图表……），
  本次合并后 fork 才是上游能力 + 本地校正的**超集**。合并前，这个 fork 在宿主 0.1.5-rc.1 上其实是**坏的**。
- **边界**：本 fork 的优势建立在"可对账、可回退、文档留痕"上——每一条与官方不同的口径都写明了理由与验证方式，
  且都能一行开关改回官方（例如把 08-17 档的 `halveCacheHit` 改成 `true`）。
