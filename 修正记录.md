# dsh-usage-dashboard 本地修正记录（过程与结果）

> 本文件夹 = 该插件的**本地工作区**（源码 + 构建产物 + 完整 git 历史 + 本文档）。
> profile 的 file: 依赖已指向本目录，任何 pnpm 操作都不会还原修复。
> 上游：https://github.com/Cassius0924/dsh-usage-dashboard （base 0.5.0，commit d7bc428）
>
> **📌 交接说明（给新工作区 / 新对话）**：本项目的一切都在本文件夹
> `~/Documents/dsh-usage-dashboard`（独立于任何 DSH 工作区，永久可访问）。
> 新会话请直接：① 打开本文件夹读本文档；② 改动 `src/**` 后按「五、下次修改的完整流程」重建部署；
> ③ 关键入口：`src/pricing.ts`（费率/时段/节假日）、`src/usage.ts`（去重/余额核算）、
> `src/client/dashboard.tsx`（UI/滚动/白屏）。测试 62/62。

---

## 一、修正背景

插件原版（0.5.0）的费用估算与 DeepSeek 开放平台实际账单偏差巨大：
初次对账时**今日消耗显示的金额与调用数都明显高于平台口径**（金额约 3 倍、调用数约 3.4 倍）。
逐项对账后定位出 3 个真实 bug + 1 个精度增强，全部已修复并验证。

## 二、修正过程（6 项）

### ① 高峰时段误判周末 —— `src/pricing.ts` `isPeak()`
- **现象**：周六 9:00–12:00 的调用被按高峰价计算。
- **根因**：`isPeak` 只按北京时间小时判断（9–12 / 14–18），不看星期与节假日。
- **修复**：先按北京时间判周末（周六/周日整天闲时）→ 再查法定节假日表
  `CN_HOLIDAYS`（2024–2026，源自国务院通知）→ 最后比对高峰窗口。
- **规则**：高峰 = 北京时间**工作日** 9:00–12:00 / 14:00–18:00；周末与法定节假日整天闲时；
  调休补班的周末按规则仍算闲时。

### ② 跨会话重复计数 —— `src/usage.ts` `fetchUsage()`
- **现象**：今日调用数虚高约 3 倍。
- **根因**：子代理（subagent）会话会**回放父会话的同一批事件**（同 messageId），
  插件把各会话简单相加 → 每条真实请求被重复计入 1+回放次数。
- **修复**：`fetchUsage` 增加跨会话 `seenMessageIds` 去重（同一 messageId 只计一次）。
- **结果**：去重后与平台口径吻合度 ~97–98%。

### ③ 缓存命中价错误减半 —— `src/pricing.ts` `halved()`
- **现象**：费用低估约 27%。
- **根因**：用户用量 ~96% 为缓存命中 token；插件把缓存价也随闲时减半（flash 0.05/M），
  实际规则是**闲时仅 input/output 半价，缓存命中价不变**（flash 0.1/M）。
- **修复**：`halved()` 不再除以 cacheHit；同步更新计价说明文案与 README。
- **结果**：修正后今日费用与平台偏差仅 2–5%。

### ④ 新增「今日消耗（平台核算）」 —— `src/balance-tracker.ts`（新增）
- **背景**：开放平台**没有用量统计 API**，消费金额无法直接拉取。
- **方案**：余额差值法 —— `今日消耗 = 今晨余额 − 当前余额 + 今日充值增量`，
  使用 `/user/balance` 的 `total` / `toppedUp` 字段，中途充值也能正确处理。
- **实现**：`trackDailyConsumption()`；基准状态文件
  `~/.dsh/dsh-usage-dashboard-balance.json` 按**北京时间零点**自动重置；
  `fetchBalance(credentials, statePath?)` 的 statePath 可注入（测试用临时路径，不污染真实状态）。
- **播种**：2026-08-22 首次播种当日基准（= 当时余额 + 平台消费，具体数值已脱敏）。
- **结果**：余额卡新增第 4 项「今日消耗（平台核算）」，与平台消费金额**精确一致**（实时）。

### ⑤ UI：打开额度标签滚回顶部 + 偶发白屏防御 —— `src/client/dashboard.tsx`
- **问题 1（总是停在设置区）**：宿主所有会话视图共用一个滚动容器
  `[data-conversation-scroll]` 且切换视图时**保留滚动位置**；聊天视图停在底部，
  切到额度就继承底部位置 → 看到「设置」卡而非「余额」卡。
  **修复**：仪表盘挂载时把共享 scroller 滚回顶部（useLayoutEffect + rAF 再断言一次）。
- **问题 2（偶发白屏）**：宿主在会话 `blank && composerPhase==="blank"`（日志加载中/空会话）
  时对整个视图区 `return null`（插件无法渲染）；另外视图切换瞬间宿主可能把 view 区算成
  **0 高 + overflow hidden** 把已挂载内容裁掉。**修复**：挂载后短暂探测根节点尺寸，
  不可见时每 150ms 触发滚动（强制 reflow 让宿主 flex 布局收敛），可见或 1.2s 预算耗尽即停。
- **生效**：纯客户端改动，硬刷新页面即加载新 bundle（或重启 dsh web）。

### ⑥ 跨天「今日消耗」不自动重置 —— `src/client/cache.ts` / `api.ts` / `dashboard.tsx` / `src/usage.ts`
- **现象**（2026-08-23 凌晨发现）：dsh 服务跨天不重启时，「今日消耗（平台核算）」一直显示
  **昨天的**总消费金额；点击刷新数值不变；重启 dsh 后才从 0 开始。
- **排查**：host 端 `trackDailyConsumption` 的零点重置逻辑本身正常——状态文件基准在当日
  00:22（即重启后第一次成功拉取）已被重写为新一天。说明 00:00–00:20 旧进程的余额请求持续
  失败（可能是跨零点 API 波动或长进程连接僵死），客户端按设计「刷新失败保留旧数据」继续展示；
  且客户端缓存与渲染都**没有「天」的概念**，把昨天拉到的数值当作「今日」冒充展示。
- **修复（三层防御）**：
  1. **缓存同日约束**：`createCache(..., { sameDayOnly: true })`（cache.ts 新增北京日 key
     `beijingDayKey`）。余额/用量条目跨天后即使 TTL 未到也不再算新鲜 → 新一天的第一次轮询
     （页面挂载 / 预热 / widget 每 60s tick）自动重新请求并触发 host 端重置，无需手动刷新或重启；
  2. **渲染兜底**：dashboard 记录余额拉取时刻 `balanceAt`，非当天拉取的载荷其「今日消耗」一律
     显示「—」（提示「当日基准尚未建立」），绝不把昨天的数字当作今日展示——即使整天请求失败；
  3. **凭证解析超时**：host `fetchBalance` 给 `credentials.resolve` 加 10s 上限（可注入），
     端点永远有界响应，杜绝刷新按钮被挂死。
- **测试**：新增 `test/cache.test.ts`（beijingDayKey / 同日约束 / localStorage 恢复路径）+
  `usage.test.ts` 增 2 例（跨天重写基线、凭证挂起有界）；**62/62 通过**。

## 三、对账数据（2026-08-22）

| 指标 | 修复前（旧代码） | 修复后（本地） | 平台（后台） | 偏差 |
|---|---|---|---|---|
| 今日调用 | 虚高约 3 倍 | 去重后接近平台 | 平台口径 | ~2–5% |
| 今日 Tokens | ~439M（重复） | ~123.7M（去重） | 126.78M | ~2.5% |
| 今日费用 | 明显偏高（高峰误判） | 估算口径 | 平台实际 | ~2–5% |
| 消费金额（平台核算） | — | **与平台一致**（余额差值法） | 权威值 | 0% |
| 余额 | — | 实时拉取 | 权威值 | 0% |

> 请求次数 / Tokens 的残余 2–5% 差异 = 本机日志之外的 API 调用（其他设备/直接调用），
> 属平台可见、本地日志不可见；插件界面已有「仅统计本机日志」提示。

## 四、文件结构与关键位置

```
src/pricing.ts          计费核心：PEAK_RATES / halved() / isPeak() / CN_HOLIDAYS(法定节假日表)
src/usage.ts            fetchUsage() 跨会话 messageId 去重；fetchBalance() 接平台核算
src/balance-tracker.ts  余额差值法（今日消耗平台核算）
src/client/locales.ts   计价/峰值说明文案（中英）
src/client/dashboard.tsx 余额卡「今日消耗（平台核算）」展示
test/pricing.test.ts    时段/节假日/费率测试
test/usage.test.ts      去重、余额差值核算测试
build-local.mjs         本地构建脚本（跳过 tsc，产物=lib/index.js + lib/client.js）
```

## 五、下次修改的完整流程（用本文件夹）

1. **改代码**：编辑 `src/**` 或 `test/**`。
2. **构建**：`cd ~/Documents/dsh-usage-dashboard && pnpm run build`（esbuild + tsc 声明；
   只改 JS 时可 `node build-local.mjs`，两者都会注入版本 footer）
3. **跑测试**：`node test/run.mjs`（当前 70/70 通过）
4. **部署**（构建产生新 inode，硬链接失效，必须重新复制 lib 4 个文件；README 已硬链接、无需复制）：
   ```bash
   SRC=~/Documents/dsh-usage-dashboard
   DST=~/.dsh/profiles/web/node_modules/@cassius0924/dsh-usage-dashboard
   cp "$SRC/lib/index.js" "$SRC/lib/index.js.map" "$SRC/lib/client.js" "$SRC/lib/client.js.map" "$DST/lib/"
   ```
5. **重启生效**：纯客户端改动可先硬刷新浏览器（⌘⇧R）；host 端改动则重启
   `dsh web`（**必须用全局模式**，勿用 `pnpm dsh web`）→ 刷新页面。
6. **提交**：`git add -A && git commit`（本文件夹是 git 仓库，历史完整）。

## 六、维护注意事项

- **法定节假日表年度更新**：`src/pricing.ts` 的 `CN_HOLIDAYS` 覆盖 2024–2026，
  国务院每年公布新年份安排后补一行（格式 `'MM-DD'`，注释已写明年份区间）。
- **升级上游**：`git fetch upstream && git merge`（本目录 remote origin = 上游），
  冲突通常只在小范围（pricing.ts），合并后重新构建 + 部署 + 重启。
- **file: 依赖**：profile `package.json` 的依赖为
  `"@cassius0924/dsh-usage-dashboard": "file:~/Documents/dsh-usage-dashboard"`，
  任何 pnpm add/remove/install 都不会还原修复；改依赖路径后需 `pnpm install`。
- **pnpm 11 坑**：`patchedDependencies` / `overrides` 放 `package.json` 会被**静默忽略**
  （安全相关），只认 `pnpm-workspace.yaml`（https://github.com/pnpm/pnpm/issues/11536）。
- **测试隔离**：余额差值核算测试使用可注入的临时状态路径，不会写真实 `~/.dsh` 状态。

## 七、合并上游 v0.6.0（2026-09-11）

上游 `v0.6.0`（tag `v0.6.0` = `edf7c0a`，基线 0.5.0 `d7bc428`）已并入本地 fork（`git merge`），
合并前的还原点 tag：`fork-before-0.6.0-20260911-182327`。

**上游带来的（全部保留）**

- **计价表 era 化**：08-17 / 09-10 两档价目表按每条记录的发生时刻自动选表；09-10 12:00 起
  Flash 降价（高峰 0.04/2/8）；09-14 12:00 起 `deepseek-v4-pro` 请求改由 V4.1-Flash 承接、按 Flash 价计费。
- **多模态卡**：`deepseek-v4-flash-vision-exp` 的图片张数 / 估算 token / 费用 + 每天趋势 + 会话排行
  （`estimateImageTokens()` 官方缩放规则，每张上限 384 token）。
- **两代宿主接口兼容**：`sessionPersistence` handle 化（`open` / `read` / `close`，循环读到空片）为主，
  旧代 `readFrom` 回落——本机 DSH 0.1.5-rc.1 走的是新代（0.5.0 在本机会报
  `persistence.readFrom is not a function`）。
- **额度 tab 挂载置顶**、插件版本 footer（`__PLUGIN_LABEL__` 由 `build.mjs` 注入）、
  计价说明表格收敛为 `deepseek-flash` / `deepseek-v4-pro` 两行 + 小字说明。

**fork 侧保留、与上游不同的口径（代码内已注明）**

1. **闲时只对 input/output 半价，缓存命中价不变** —— 上游 0.6.0 的 `halved()` 仍把 cacheHit 一起减半；
   本 fork 按开放平台实际账单核对后不改（`src/pricing.ts` `halved()`）。README 单价表据此写作
   flash 高峰/闲时 `0.04 / 0.04`、pro `0.3 / 0.3`，测试里的闲时 cacheHit 期望值同步改回高峰价。
2. **周末与法定节假日全天闲时** —— 上游 era 的第一档仍写 "every day"；2026-08-22（周六）的平台账单
   按闲时计费，故 `isPeak()` 一律先判周末 / `CN_HOLIDAYS`，再走 era 的 `weekdaysOnly`。
3. **fork 独有功能**：跨会话 `messageId` 去重、余额差值法「今日消耗（平台核算）」、跨天自动重置、
   额度页白屏轻推——上游均无，合并后全部保留。

**冲突（6 个文件）与处理**

| 文件 | 冲突 | 处理 |
|---|---|---|
| `src/pricing.ts` | 3 | 头部注释取并集；价目表用上游命名 `PEAK_RATES_08_17` + fork 注释；`isPeak()` 用 fork 规则（周末/节假日优先） |
| `src/usage.ts` | 2 | import 取并集；回调 = 上游 vision 累积器 + fork 去重 |
| `src/client/locales.ts` | 4 | 文案取并集，删掉已无引用的 `pricing.switchNote` |
| `src/client/dashboard.tsx` | 1 | 挂载置顶：保留 fork 白屏防御 + 上游 `props.sessionId` 依赖 |
| `test/pricing.test.ts` | 2 | helper 兼容两种签名；用例取并集；闲时 cacheHit 期望值按 fork 口径修正 |
| `README.md` | 1 | 计价条目取并集；单价表按 fork 口径修正 |

另：`build-local.mjs` 补上 `__PLUGIN_LABEL__` define，与 `build.mjs` 对齐（否则本地构建的 client
缺版本 footer 常量）。

**验证与部署**

- `node test/run.mjs`：**70/70 通过**（fork 62 + 上游新增 8）。
- `tsc --noEmit`（主 + 测试）exit 0；`pnpm run build`（esbuild + d.ts）exit 0，
  `lib/client.js` 内含 `dsh-usage-dashboard v0.6.0`。
- 部署：备份 `dsh-auto-20260911-182629` → 复制 `lib/` 与 `package.json` 到
  `~/.dsh/profiles/web/node_modules/@cassius0924/dsh-usage-dashboard/`（`diff -r` 一致，安装副本版本 0.6.0）
  → 重启 `dsh web` 后生效（host 端有改动：handle 化读取 + vision 统计）。
