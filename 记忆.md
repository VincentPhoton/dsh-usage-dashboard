# 工作区记忆（跨会话速查）

> 本文件 = 项目长期记忆的**工作区落盘版**，与 Mnemon 记忆、热记忆(MEMORY.md)同源同步。
> 新会话快速恢复上下文：先读本文件速查 → 细节再查《修正记录.md》。
> 详细修复过程、对账数据见《修正记录.md》；本文只保留「必须知道」的浓缩事实。
> 建立时间：2026-08-22

---

## 一、项目速查卡

| 项 | 内容 |
|---|---|
| 项目 | DSH Web 用量统计插件 `@cassius0924/dsh-usage-dashboard`（上游 base 0.5.0, commit d7bc428） |
| 工作区 | `~/Documents/dsh-usage-dashboard`（独立 git 仓库：源码 + lib 构建产物 + 完整历史） |
| profile 依赖 | `"file:~/Documents/dsh-usage-dashboard"` —— **任何 pnpm 操作都不会还原本地修复** |
| 关键入口 | `src/pricing.ts`(费率/时段/CN_HOLIDAYS)、`src/usage.ts`(去重/余额)、`src/balance-tracker.ts`(平台核算)、`src/client/dashboard.tsx`(UI/滚动/白屏)、`build-local.mjs`(构建) |
| 测试 | `npm test`（当前 57/57） |

## 二、修改 → 部署标准流程

1. 改 `src/**` 或 `test/**`
2. `node build-local.mjs`（跳过 tsc）
3. `npm test`
4. 复制 lib **4 个文件**（构建产生新 inode，硬链接失效**必须重复制**；README 已硬链接无需复制）：
   ```bash
   SRC=~/Documents/dsh-usage-dashboard
   DST=~/.dsh/profiles/web/node_modules/@cassius0924/dsh-usage-dashboard
   cp "$SRC/lib/index.js" "$SRC/lib/index.js.map" "$SRC/lib/client.js" "$SRC/lib/client.js.map" "$DST/lib/"
   ```
5. 生效：纯客户端改动硬刷新浏览器(⌘⇧R)；host 端改动重启 `dsh web`
6. `git add -A && git commit`

## 三、环境硬规则（必须遵守）

- ⛔ **启动 DSH 一律全局模式 `dsh web`**（node /opt/homebrew/bin/dsh）；`pnpm dsh web` 会使 fail-soft 内核补丁失效，**禁止再用**。
- 更新 DSH：`npm i -g @deepseek-ai/dsh`。⚠️ npm 上单独的 `"dsh"` 包是**冒名 JS shell**，切勿安装。
- 源码仓库 `~/Documents/deepseek-harness` 仅作备份保留。

## 四、运维与环境事实

- **配置自动备份**（2026-08-22 建）：手动脚本 `~/Documents/DSH备份/backup-dsh.sh`（带时间戳，保留最近 10 份）；web profile 的 package.json 有 **preinstall 钩子**，每次 pnpm add/install/remove 插件操作前自动备份到 `~/Documents/DSH备份/dsh-auto-<时间戳>/`（profile-web-config.tar.gz + settings.yaml + note.txt）。
- **插件装卸流程**：安装 = `pnpm add -w <pkg>` + 包名追加进 package.json 的 `dsh.profile.bundles` + 重启 dsh web + boot manifest 验证；卸载反向。peer 依赖问题在 **pnpm-workspace.yaml** 配 `peerDependencyRules`（官方注入包用 ignoreMissing+allowedVersions）。
- **pnpm 11 坑**：`patchedDependencies` / `overrides` 放 package.json 会被静默忽略（安全相关），只认 pnpm-workspace.yaml。
- **节假日表年度维护**：`src/pricing.ts` 的 `CN_HOLIDAYS` 覆盖 2024–2026，国务院公布新年份后补一行 `'MM-DD'`。
- **升级上游**：`git fetch upstream && git merge`（origin=上游）→ 重建 → 部署 → 重启；冲突通常只在 pricing.ts 小范围。

## 五、已修复 5 项（详见《修正记录.md》）

1. 高峰时段 = 北京时间**工作日** 9-12/14-18；周末与法定节假日整天闲时。
2. 跨会话按 messageId 去重（修 subagent 回放导致调用数虚高 ~3 倍）。
3. 闲时仅 input/output 半价，**缓存命中价不变**（flash 0.1/M / pro 0.3/M）。
4. 余额卡「今日消耗(平台核算)」= 余额差值法（今晨余额−当前余额+今日充值增量）；基准文件 `~/.dsh/dsh-usage-dashboard-balance.json` 按北京时间零点重置；2026-08-22 已播种当日基准（数值已脱敏）。
5. UI：额度标签挂载滚回共享滚动容器 `[data-conversation-scroll]` 顶部 + 白屏可见性探测防御（150ms 轻推 / 1.2s 预算）。

**对账口径**：请求/Tokens 只能本机日志精确口径（残余 2–5% 差异 = 非本机调用）；余额与消费金额与平台 0% 偏差。

## 六、git 提交锚点

`b7bfec6`(时段) → `184325e`(去重+缓存费率) → `2083019`(余额差值核算) → `64abbe6`(修正记录) → `c55e3e7`(UI 滚动/白屏) → `b1b12da`(文档补 UI 项)

## 七、记忆来源索引

- 本地：《修正记录.md》（全过程）、《README.md》
- Mnemon Documents：「dsh-usage-dashboard 项目工作区交接与修复笔记(2026-08-22)」、「DSH Web Profile 插件运维与修复笔记(2026-08-22)」
- Mnemon 主记忆空间 `default`（mnemon-native）：持久洞察若干条
- 热记忆 MEMORY.md：已同步压缩指针；若发现为空，可按本文件恢复
