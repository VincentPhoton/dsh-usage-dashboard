# 工作区记忆（跨会话速查）

> 本文件 = 项目长期记忆的**工作区落盘版**，与 Mnemon 记忆、热记忆(MEMORY.md)同源同步。
> 新会话快速恢复上下文：**先读 [AGENTS.md](./AGENTS.md)**（DSH 会自动把它作为工作区指令注入）
> → 本文速查 → 全过程看《项目全记录.md》→ 修正细节看《修正记录.md》。
> 建立 2026-08-22；最近更新 2026-09-11（合并上游 0.6.0 + 官方价格表对齐）。

---

## 一、项目速查卡

| 项 | 内容 |
|---|---|
| 项目 | DSH Web 用量统计插件 `@cassius0924/dsh-usage-dashboard` 本地 fork（上游 base 0.6.0，tag `v0.6.0` = `edf7c0a`） |
| 工作区 | `~/Documents/dsh-usage-dashboard`（独立 git 仓库：源码 + lib 产物 + 完整历史） |
| 当前提交 | `321c760`（官方价格表对齐）→ 见 `git log -1`（性能与逻辑优化，89/89 测试） |
| profile 依赖 | `"file:~/Documents/dsh-usage-dashboard"` —— 任何 pnpm 操作都不会还原本地修复 |
| 关键入口 | `src/pricing.ts`(费率/时段/era/`halveCacheHit`/`CN_HOLIDAYS`)、`src/usage.ts`(折叠记录/去重/两代 persistence 读取)、`src/session-cache.ts`(revision 增量缓存)、`src/balance-tracker.ts`(平台核算+跨午夜基线)、`src/client/dashboard.tsx`(UI/滚动/白屏/多模态卡/footer/SyncStatus)、`src/client/poll.ts`(可见性轮询)、`src/client/cache.ts`(同日缓存约束)、`build.mjs`(完整构建)/`build-local.mjs`(快构建) |
| 测试 | `node test/run.mjs`（当前 **89/89**） |
| 宿主 | DSH 0.1.5-rc.1（`sessionPersistence` 已 handle 化，插件按代兼容新旧接口） |

## 二、修改 → 部署标准流程

1. 改 `src/**` 或 `test/**`
2. `pnpm run build`（esbuild + tsc 声明；只改 JS 时 `node build-local.mjs` 也可，两者都会注入版本 footer）
3. `node test/run.mjs`
4. **先备份**（preinstall 钩子不一定触发）：`bash ~/Documents/DSH备份/backup-dsh.sh "自动:..."`
   然后部署（构建产生新 inode，硬链接失效，**必须重新复制**整个 `lib/` 与 `package.json`）：
   ```bash
   SRC=~/Documents/dsh-usage-dashboard
   DST=~/.dsh/profiles/web/node_modules/@cassius0924/dsh-usage-dashboard
   rm -rf "$DST/lib" && cp -R "$SRC/lib" "$DST/lib"
   cp "$SRC/package.json" "$SRC/README.md" "$SRC/LICENSE" "$SRC/cordis.patch.yml" "$DST/"
   ```
5. 生效：纯客户端改动硬刷新浏览器(⌘⇧R)；host 端改动重启 `dsh web`
6. `git add -A && git commit`

## 三、环境硬规则（必须遵守）

- ⛔ **启动 DSH 一律全局模式 `dsh web`**（node /opt/homebrew/bin/dsh）；`pnpm dsh web` 会使 fail-soft 内核补丁失效，**禁止再用**。
- 更新 DSH：`npm i -g @deepseek-ai/dsh`。⚠️ npm 上单独的 `"dsh"` 包是**冒名 JS shell**，切勿安装。
- 源码仓库 `~/Documents/deepseek-harness` 仅作备份保留。

## 四、运维与环境事实

- **配置自动备份**（2026-08-22 建）：手动脚本 `~/Documents/DSH备份/backup-dsh.sh`（带时间戳，保留最近 10 份）；
  web profile 的 package.json 有 **preinstall 钩子**，插件操作前自动备份到 `~/Documents/DSH备份/dsh-auto-<时间戳>/`。
  ⚠️ 钩子不一定触发（2026-09-09 的一次 pnpm add 就没触发）——每次操作后人工确认有新快照，没有就手动补。
- **插件装卸流程**：安装 = `pnpm add -w <pkg>` + 包名追加进 package.json 的 `dsh.profile.bundles` + 重启 dsh web + boot manifest 验证；卸载反向。
  peer 依赖问题在 **pnpm-workspace.yaml** 配 `peerDependencyRules`。
- **pnpm 11 坑**：`patchedDependencies` / `overrides` 放 package.json 会被静默忽略（安全相关），只认 pnpm-workspace.yaml。
- **节假日表年度维护**：`src/pricing.ts` 的 `CN_HOLIDAYS` 覆盖 2024–2026，国务院公布新年份后补一行 `'MM-DD'`。
- **会话日志格式**：`~/.dsh/sessions/<cwd>/<id>/session.v3.jsonl.zstd` 是 **append-only 多帧 zstd**
  （node 的 `zstdDecompressSync` 只解第一帧，要用 `zstd -dc` 或逐帧解）；`session/end-seed` 是
  **恢复边界**，不是 fork 切点。
- **revision 缓存前提**：`list()`/`stat()` 的 `revision`（stat 派生）必须存在才缓存；jsonl 后端**没有**
  `eventCount`，所以"按偏移续读"暂不可做。宿主 `list()` 明确不保证顺序。
- **余额状态文件形状**：`~/.dsh/dsh-usage-dashboard-balance.json` 现含
  `estimatedBaseline / sampleAt / sampleTotal / sampleToppedUp`；**旧格式一律按无基线重建**。
- **升级上游**：`git fetch upstream --tags && git merge <tag>` → 重跑测试 → **逐条核对 fork 口径有没有被上游改写** → 重建 → 部署 → 重启。
  冲突通常集中在 `src/pricing.ts`、`src/usage.ts`、`src/client/locales.ts`、`src/client/dashboard.tsx`、`test/pricing.test.ts`、`README.md`。

## 五、fork 与上游的口径差异（关键）

1. **闲时减半规则**：09-10 及以后 → 三项全减半（官方口径，flash 空闲 0.02/1/4）；**08-17–09-10 档 → 缓存命中价不减半**
   （当年按实际账单核对出的口径）。由 `PriceEra.halveCacheHit` 控制，一行可改，详见《项目全记录.md》§6。
2. **周末与法定节假日全天闲时**（官方原文字面只写"周一至周五"，节假日这一条是本 fork 的额外口径，尚无账单验证）。
3. **fork 独有功能**：跨会话 `messageId` 去重（归属确定化：按 `delegationDepth → createdAt → id` 升序取祖先副本）、
   余额差值法「今日消耗（平台核算）」、跨天自动重置、凭证 10s 有界超时、额度页白屏轻推、
   **revision 增量折叠缓存**（`/usage` 与 `/session` 共用；无 revision 则绝不缓存）。

**对账口径**：请求数/tokens 以本机会话日志为精确口径（残余 2–5% 差异 = 非本机调用）；余额与消费金额与平台 **0% 偏差**。

## 六、git 提交锚点

`b7bfec6`(时段) → `184325e`(去重+缓存费率) → `2083019`(余额差值核算) → `64abbe6`(修正记录) → `c55e3e7`(UI 滚动/白屏)
→ `2509337`(跨天重置) → `2f39ad8`(fork 声明) → `c452114`(合并上游 0.6.0) → `321c760`(官方价格对齐)
→ `ff56874`(AGENTS.md) → 性能与逻辑优化（去重归属确定化 / 反事实档位按 now / 跨午夜基线 /
   revision 增量缓存 / SyncStatus 拆分 / widget 轮询降频；测试 89/89，详见《修正记录.md》§九）

还原点：tag `fork-before-0.6.0-20260911-182327`；备份 `dsh-auto-20260911-182629`、`dsh-auto-20260911-184217`

## 七、记忆来源索引

- 本地：《项目全记录.md》（全过程总览）、《修正记录.md》（修正过程与对账）、《README.md》
- Mnemon Documents：「dsh-usage-dashboard 项目工作区交接与修复笔记(2026-08-22)」、「DSH Web Profile 插件运维与修复笔记(2026-08-22)」
- Mnemon 主记忆空间 `default`（mnemon-native）：持久洞察若干条
- 热记忆 MEMORY.md：已同步压缩指针；若发现为空，可按本文件恢复
