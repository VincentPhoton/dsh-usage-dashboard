# DESIGN NOTES — 设计决策与约定

## 视觉语言

- **一切颜色走 DSH 的 CSS 变量**，第二参数是回退值：`var(--dsw-alias-label-primary, #1f2328)`。
  本机 GUI 是浅色主题，插件不得硬编码深色底，也不得引入自己的主题色系。
- 常用变量：`--dsw-alias-bg-layer-1/2`、`--dsw-alias-bg-overlay`、`--dsw-alias-border-l1/l2`、
  `--dsw-alias-label-primary/secondary/tertiary`、`--dsw-alias-state-business-primary`（主色/链接）、
  `--dsw-alias-state-success-primary`、`--dsw-alias-state-error-primary`。
- 卡片：`border-radius:12px`，1px 边框，`padding:16px`；卡片标题 12px / 600 / 次级色。这是全部卡片的基线，
  额外的「主卡片」权重是在此基线上的加法修饰，不是另开一套规范。
- 数字一律 `font-variant-numeric: tabular-nums`，避免刷新时抖动。
- 类名前缀：悬浮窗 `dsh-quota-*`，仪表盘 `dq-*`。全部样式集中在 `src/client/styles.ts` 一张表里。
- **小字号两档体系（轮次 41 收敛）**：全站小字号曾经在 11-12.5px 区间里塞了 11/11.5/12/12.5px 四个值，
  0.5px 步进会被浏览器亚像素渲染抹平，"数值上分四层、视觉上只有一层"，只留下维护负担。现收敛为两档
  整数像素、间隔完整 1px：**11px＝"微标签"**——用户一眼扫过而不逐字读的短内容（dt 风格的统计/周期标签、
  徽章、状态胶囊、占比数字、极简标签），如 `.dq-stat-label`、`.dq-period-label`、`.dq-delta`、
  `.dq-pricing-now`、`.dq-coverage-status`、`.dq-rank-share`；**12px＝"次要正文"**——需要被完整阅读的
  说明性文字，涵盖小节标题（`.dq-card-title`、`.dq-budget-title`、`.dq-chart-title`）、按钮/分段控件
  文本（`.dq-export-btn`、`.dq-model-btn`、`.dq-chart-switch-btn`、`.dq-window-btn`——同为"分段控件/
  过滤按钮"家族，统一到一档后不再各写各的数值）、legend/hint/footnote/table 类说明文本
  （`.dq-setting-hint`、`.dq-toggle-hint`、`.dq-coverage-brief`、`.dq-pricing-table` 等），以及告警/
  空态整句（`.dq-alert`、`.dq-empty`）。两档内部仍靠 `font-weight`/取色区分标题与正文（如 12px 标题
  600 权重、正文多为 400-500），字号本身不再承担"标题比正文小 0.5px"这种不可辨识的职责。**这个区间以后
  新增样式时不要再引入 11.5/12.5 这类中间值**；如果确实需要和这两档拉开辨识度，去复用已经建立的其它
  独立档位（10px 图标/箭头类字符、13px 起的 hero 邻近文本），不要在 11-12.5px 内再开新档。这两档之外
  的字号各自有明确、独立的语义理由，不属于这次合并范围：`.dq-card--primary .dq-card-title` 13px/700
  （轮次 36，主/次卡片权重）、`.dsh-quota-collapsed-total` 15px（轮次 34，悬浮窗折叠总额）、
  `.dq-usage-totals .dq-stat-value` 在 620px 媒体查询内的 13px（轮次 38，防止压力数据换行）、
  `.dq-remaining`/`.dq-period-cost` 22px（轮次 35，hero 数字）。

## 信息层级（仪表盘）

1. 余额（最大号数字，24px 级）
2. 今日 / 本月消耗（用户第二关心）
3. 累计用量统计 + 图表
4. 官方链接、开关等配置项沉底

- 数据完整性属于用量图表的审计信息，放在图表底部的轻量 disclosure 中；折叠态就要看见扫描规模与状态，
  展开后再呈现失败 / 跳过明细和本机日志口径，避免再增加一张同权重卡片抢占首屏。
- 仪表盘「hero 数字」（余额、消耗概览今日 / 本月 / 累计）统一复用 `22px / font-weight:680 / letter-spacing:-.01em`
  这一档（源头定义在 `.dq-period-cost`，余额数字的 `.dq-remaining` 直接抄同样四个值）；其余 `.dq-stat-value`
  复用场景（用量分解数字、缓存命中率、预计可用天数、状态文字等）维持 `18px/650` 不变。新增任何"这是本卡片
  最重要的数字"场景时，优先复用这一档字号字重组合，不要再新开一档只用一次的字号。
- 同一张卡片内的一组同类对比指标不重复套浅底圆角小卡；用留白与 1px 分隔线表达比较关系，窄屏把垂直分隔
  转成水平分隔，减少“卡片套卡片”的视觉噪音。
- 卡片容器建立两档视觉权重（轮次 36，原 9 张卡片，轮次 39 合并官方平台后为 8 张，轮次 40 拆分 DSH 用量卡后
  回到 9 张），依据本节层级 1-3 与 4 划界，不新增第三档：**主卡片**＝账户余额 / 消耗概览 / 用量趋势
  （层级 1-3，首屏必读），加 `.dq-card--primary` 修饰类，在 `.dq-card` 基线上叠加 `padding:20px`
  （基线 16px）、边框换用更深的 `--dsw-alias-border-l2`（基线 `--dsw-alias-border-l1`）、标题升到
  `13px/700/label-primary`（基线 `12px/600/label-secondary`）；**次级分析卡片**（用量热力图、高峰/闲时
  分布、缓存命中与节省、模型成本排行、会话成本排行——深入分析性质、非首屏必读）与**导航/配置卡片**（设置，
  层级 4，轮次 39 起内含原「官方平台」3 个链接）都不加修饰类，维持 `.dq-card` 原样，天然比主卡片轻。
  轮次 40 把原「DSH 用量」拆成「用量趋势」（用量总数统计 + 逐天/逐小时图表 + 模型筛选/统计周期/指标切换 +
  覆盖诊断，沿用原卡的 primary 权重）与「用量热力图」（近一年热力图单独成卡，判定为次级分析卡而非主卡——
  它和高峰/闲时分布、缓存命中这些"深入分析、非首屏必读"的图表同一性质：日常最需要盯的是可变周期的用量趋势
  与总数，近一年热力图是长期模式的辅助浏览，不是每次打开都要第一眼看的数字，且它的固定近一年口径与主卡
  1-3 的"可变周期"语境本就不同）。两条 `.dq-card--primary` 规则都只调已有排版属性（内边距 / 标题字号字重
  取色 / 边框深浅变量档位），不新增背景色板或强调色；未来新增卡片时先判断信息层级属于 1-3 还是 4，再决定
  要不要挂这个修饰类，不要为中间层级另开新档。
- **信息量太小的导航类卡片要并入同层级的相邻卡片，而不是继续独占一整张卡位**（轮次 39）：「官方平台」
  只放 3 个链接按钮，高度约 99.5px（全站最矮），却背着和信息密集卡片同规格的边框 + 标题 + 16px 内边距，
  在轮次 36 建立主/次卡片权重分级后这个成本不对称更明显（它和「设置」是仅存的两张不参与分级的导航/配置
  类卡片，但信息量远够不上"一整张卡片"的份量）。处理手法：把 3 个链接整体搬进「设置」卡内部，在卡标题
  下加一个 12px/600/label-primary 的小节标题（复用 `.dq-budget-title`/`.dq-coverage-title` 已定的字号
  字重取色组合，不新开字号档），链接区与后面的开关/预警线/预算设置之间用「设置」卡本已在用的
  `margin-top:14px;padding-top:14px;border-top:1px solid --dsw-alias-border-l1` 分节手法隔开——
  直接在只用了一次的 `.dq-toggle` 选择器上加这三条属性，不新建包裹 div 或新类名。卡片总数 9→8，顺序
  与内容归属不变（原「官方平台」位置消失，链接内容移进原「设置」卡）。**（措辞更正 + 轮次 39 修复轮 1）**：
  上面"复用 `.dq-budget-title`/`.dq-coverage-title` 已定的字号字重取色组合"这句只是复用了三个属性值，
  `.dq-links-title` 自己还多加了一条这两个选择器都没有的 `margin:0 0 8px`（用于和下方链接行拉开间距），
  原表述把"三属性同值复用"说成了"组合完全复用"，略有夸大，这里予以澄清。另外独立 Critic 指出小节标题
  「官方平台」在「设置」卡内出现有语义跳跃感（用户会困惑"为什么设置卡里冒出官方平台"）——已把
  `links.title` 改名为「快捷入口」/ `Quick Links`（`src/client/locales.ts`），更贴合"这是设置卡内的一个
  导航子区"的语境，不再借用外部品牌名；样式、DOM 结构、3 个链接的 `href`/文案均未变。
- **账户全局维度 vs 会话维度是两个独立的统计口径，卡片标题必须各自明确写出范围**（轮次 45）：「额度」
  tab 本身是 `conversation.view` slot，和「对话」「对话轨迹」同挂在具体会话内部，但 tab 里绝大多数卡片
  （余额、消耗概览、用量趋势、模型/会话排行……）统计口径都是账户全局（跨全部本机会话日志），只有新增的
  「当前会话消耗」卡是这个具体会话专属的口径。两种口径的数字量级完全不可比（一个会话的费用通常远小于
  账户累计），如果标题不写清楚范围，用户很容易把"这个会话花了多少"和"账户一共花了多少"看混——因此新卡
  标题必须带「当前会话」四个字（`当前会话消耗（费用为估算）`），紧跟在「账户余额」卡之后、「消耗概览
  （费用为估算）」卡之前，两个标题在同一屏内可以直接对照阅读，不靠用户记忆区分。以后再新增任何"会话
  维度"而非"账户维度"的卡片，标题都要延续这个"显式写明统计范围"的约定，不能假设位置或上下文能替代
  文字说明。
- **会话维度卡片同样可以是主卡片，"主卡片"不是"账户全局层级 1-3"的同义词**（轮次 45）：轮次 36 建立的
  主/次卡片两档权重原本按 DESIGN_NOTES「信息层级」1-3（账户全局）与 4（导航/配置）划界，「当前会话
  消耗」卡不属于这个账户全局层级序列的任何一级，而是与之正交的另一个维度——但它同样是打开这个 tab 时
  上下文最直接相关、首屏必读的数字，因此同样判给 `.dq-card--primary`。主卡片从 3 张（余额/消耗概览/
  用量趋势）增到 4 张（余额/当前会话消耗/消耗概览/用量趋势）。这条先例说明"是否给 primary"的判断依据
  应该是"这张卡对当前上下文而言是不是首屏必读"，而不是死板地对照"信息层级 1-3 的固定清单"——层级清单
  本身可以随着新维度的加入而扩展，不需要为了迁就旧清单把明显首屏必读的新卡片降级成次级卡。
- 留白节奏遵循「卡片间距 > 卡内小节间距 > 卡内行内间距」三档级差（轮次 37，Critic 复核后修正）：
  `.dq-balance{gap:24px}` 表达"卡片与卡片之间"，比卡内小节分隔（`margin-top:12/14/16px`，如
  `.dq-budget`/`.dq-pricing`/`.dq-setting`/`.dq-coverage` 这类带 `border-top` 的小节分隔，以及
  `.dq-chart-title`/`.dq-chart-block-head` 这类用量趋势卡内 daily/hourly 图表、用量热力图卡内热力图
  （轮次 40 起分卡，样式类沿用同一套）的小节标题起始处的间距）至少大 1.5 倍，比卡内行内元素间距（`.dq-card-head`/`.dq-session-head` 这类
  同一行内控件的 `gap:8~12px`）更是明显更疏；卡片自身的 `padding`（`.dq-card` 16px /
  `.dq-card--primary` 20px，轮次 36 已定）表达"卡片内部边界的呼吸感"，与"卡片之间的间距"是两件
  不同的事，不要用同一个数值表达，也不要在新增卡内小节或行内控件间距时贴近甚至超过卡片间距的数值，
  否则"卡片外"和"卡片内"会重新失去区分度。`.dq-chart-title`/`.dq-chart-block-head` 原为 `margin-top:
  18px`，是本轮初次排查漏看的例外档（24/18≈1.33 倍，不满足"至少 1.5 倍"），已收到 16px 并入既有卡内
  小节最大档，不新增独立档位；对 `src/client/styles.ts` 里所有 `margin-top`/简写 `margin` 顶部分量
  做过一次完整 grep 复核，卡内小节档（带 `border-top` 分隔或图表/热力图小节标题）目前统一落在
  12/14/16px 区间，其余均为卡内行内间距（2~14px，无 `border-top`，用于同一小节内的行间/元素间距），
  未发现其它超过 16px 的小节档。

## 交互约定

- 慢接口（usage ~4.8s）必须区分三态：**加载中用骨架**、**空数据用空态文案**、**刷新失败保留旧数据 + 提示**。
  绝不用空态文案冒充加载态。
- 悬浮窗：拖动中关闭 transition（`.dsh-quota-dragging{transition:none}`），松手再吸附时开启，
  缓动统一 `cubic-bezier(.22,1,.36,1)`，时长 `.28s`。
- 悬浮窗折叠态的标题行（抓手 / 状态点 / 标题 / 操作按钮）不与数值类次要信息共享同一行：flex 子项默认
  `flex-shrink:1`，一旦行内塞进第三种内容（如折叠总额），最先被压缩的往往是承担"这是什么控件"身份识别
  职责的标题文本，而不是视觉上更次要的数字。次要数值改放到标题行下方的独立一行（如 `.dsh-quota-collapsed-total-row`），
  折叠是为了省空间，不能连带把身份信息一起省掉。
- 系统设置 `prefers-reduced-motion: reduce` 时，插件两个根节点内的动画、过渡与平滑滚动全部关闭；规则放在
  样式表末尾并限定作用域，既避免被后续声明覆盖，也不影响 DSH 宿主界面。
- 用户设置类状态（悬浮窗显示/位置/收起、阈值、预算）一律持久化到 localStorage，
  key 统一前缀 `dsh-usage-dashboard:`，并带 `:v<N>` 版本后缀便于演进。
- **会话维度的抓取数据（fetched data，不是用户设置）不接入 localStorage 持久化缓存**（轮次 45 决策）：
  `balance`/`usage` 两个账户全局接口走 `cache.ts` 的 `createCache()`——5-60 秒到 5 分钟 TTL + 镜像一份到
  localStorage，目的是让"重开这个账户"时首屏不用等网络。但"当前会话消耗"这类以 `sessionId` 为 key 的
  数据本质不同：它只在这一个会话内有意义，会随会话切换、随这个会话新产生的用量而变化，不是"重开同一个
  账户"这种可复用的稳定状态。如果也套用跨刷新持久化，要么要给每个打开过的会话都存一份缓存（成本随会话
  数线性增长，且大多数只会被读一次），要么只存"最近一个"（刷新页面后极易显示错会话的旧数字，且要额外
  处理"缓存的 sessionId 和当前 sessionId 不一致"的失效判断）。两条路都比"每次挂载就现拉、只做内存级
  in-flight 去重"（`src/client/api.ts` 的 `fetchSessionUsage`）更复杂、风险更高，换来的收益却只是省下
  一次很便宜的单会话读盘请求（host 侧不做全量重放，成本远低于 `usage` 接口）。这也顺带让 LEARNINGS 里
  "持久化缓存必须校验结构"那条坑天然不适用于这份数据——它压根不写 localStorage，没有"旧版本缓存被新代码
  当作可信结构读出来"的风险面。以后任何新增的、以某个动态 id（会话/消息/文件……）为 key 的抓取数据，
  默认按这条先例处理：不进 `cache.ts`，除非有具体证据证明"跨刷新复用"的收益大于"按 id 分别失效"的复杂度。
- 悬浮窗只用 `position:absolute` 挂在 `[data-shell-overlay]` 上，拖动边界排除侧边栏与会话头部。
- 弹出式菜单打开后聚焦首个选项；Escape 关闭并回焦触发按钮；触发按钮始终同步 `aria-expanded`，
  菜单内选择后不强制关闭，便于多选连续操作。
- 解释型明细不能只靠 hover；优先使用原生 `details / summary`，同时保留桌面 hover 预览，并确保点击、触屏、
  Enter / Space 与 `:focus-visible` 都能完成同一任务。
- **列表每一行的"展开查看完整明细"同样优先用原生 `details / summary`，哪怕展开内容很重（整张卡片级的
  组件），而不是为列表行另开一套 `aria-expanded` 按钮状态机**（轮次 46，会话成本排行加展开按钮）：
  `.dq-pricing`/`.dq-coverage`/`.dq-remaining`/`.dq-runway` 这几个既有 disclosure 都是"一行文字展开一小
  段说明"，本轮「会话成本排行」每行展开后渲染的是轮次 45 「当前会话消耗」卡片同款的完整
  `SessionUsageCard`（headline 数字 + 按模型拆分 + 活跃时间范围），内容量明显更重，但键盘可达
  （Tab 到 `<summary>`，Enter/Space 切换）、触屏点按、`:focus-visible` 环这些能力原生 `<details>`
  全部免费提供，不需要因为"内容变重了"就换成手写的按钮 + 条件渲染 + 状态同步。做法是把原有的多行内容
  （标题/费用/占比条/tokens/调用/时间）整体搬进 `<summary>`（外层包一个 flex body div 放在自绘的旋转
  `▸` chevron 旁边），展开区 `<div>` 挂在 `<summary>` 后面渲染详情——`<summary>` 内允许多行/多个子元素，
  不是只能放一小段行内文字。这条先例把"原生 disclosure 优先"的适用范围从"一小段说明文字"明确扩大到
  "列表行展开出一整张同构卡片"，以后再遇到类似"列表某一行需要展开出更多同类型信息"的需求，先考虑这个
  模式，不要默认它"内容太多，必须换成受控组件"。
- **列表行展开出的详情如果和别处已有的卡片是同一种信息（同一份数据结构、同一套统计口径），必须原样
  复用那张卡片的渲染组件，不写第二份展示逻辑**（轮次 46）：本轮「会话成本排行」展开区直接
  `<SessionUsageCard data={...} />`——和轮次 45 「当前会话消耗」卡片是同一个函数、同一份 JSX，唯一
  区别是数据来源（一个是当前会话的 `sessionId`，一个是排行榜某一行的 `s.id`）和请求时机（挂载即取 vs
  首次展开才取）。三态判断（loading / error / data）也刻意做成和「当前会话消耗」卡片逐一对应的结构，
  这样以后两处任何一处的展示逻辑演进，另一处不会因为"看起来像但其实是抄的两份代码"而悄悄跑偏。
- 统计周期属于用量趋势卡、模型排行、会话排行三块共享上下文（用量热力图卡固定近一年，不受此控件影响，
  轮次 40 拆卡时随之不放置该控件）；控件放在用量趋势卡头部，两个排行标题必须重复当前周期，
  避免用户滚动后失去口径。365 天逐日图允许卡片内部横向滚动，但页面本身不能横向溢出，初始位置对齐最新日期。
- 图表指标控件紧邻模型过滤与逐天 / 逐小时切换；选中态沿用同一分段控件语言。柱高和峰值单位跟随指标，
  但 tooltip 不裁掉其他指标，保证每个数据点仍可解释。
- 图表使用 roving tabindex：每张图只有最近有数据的数据点进入页面 Tab 序列，方向键在点间移动，Home / End 到首尾；
  热力图左右按周、上下按日。焦点、tooltip 与 `aria-label` 必须同步，底部点位滚入固定输入框上方的安全区。
- 620px 及更窄视口下，`.dq-balance` 根容器的底部 padding 会实时避让 DSH 固定输入框：`dashboard.tsx` 用
  `src/client/dom.ts` 里与 widget.tsx 共用的 `[data-slot="conversation.composer.dock"]` 座位测量法，把测得
  的实际高度写成 CSS 变量 `--dq-composer-h`，`styles.ts` 只在 620px 媒体查询里消费它（`calc(该变量 + 16px)`）；
  桌面 `padding:20px 24px 48px` 完全不引用这个变量，因此永不受影响。新增会覆盖窄屏底部的常驻元素（例如未来
  可能的悬浮提示条）时，复用同一变量而不是另起一个固定像素留白，避免两处安全区各算各的。
- 触屏没有 hover：图表数据点改为点按固定 tooltip（再点同点或图表外区域关闭），不再靠 `pointermove` 触发；
  用位移 + 耗时阈值区分点按与滚动/拖动，不调用 `preventDefault`，让横向/纵向滚动保持原生。固定态与键盘
  roving 焦点态各自独立又共享同一 tooltip 展示逻辑，判定逻辑集中在 `chart-focus.ts` 的纯函数里，不另起
  并行状态机。
- 不基于本机短周期会话日志显示“异常消耗”告警或风险色：数据覆盖不完整且使用波动天然很大，
  任何弱基线都会产生误导。趋势与原始指标可以如实呈现，但不替用户下风险结论。
- i18n 使用 DSH 原生 `locale` 服务与全局「Settings → Language」设置，不在插件里重复造语言开关；
  slot 注册声明 namespace，tab / overlay 从宿主接收响应式 `t`，内部组件统一从翻译上下文取词。
  新增用户可见文案必须进入中英文同键词典，词典测试校验键、占位符与英文残留。

## 计价

- 费用是**估算**，UI 必须让用户看得到「按什么价算的」。
- 价目表按 model 名匹配，未知模型回退到 pro 价并在说明里标注。
- 2026-08-17 起 DeepSeek 峰谷定价：按事件本地时间判定时段，生效日之前的事件仍用旧价。
